###Special / built-in variables
$0   # name of the script itself = bash command
--------
$1 $2 $3  # positional args passed to script or function
hello.sh file 
............
#!/usr/bin/env bash #<- best practice shebang script, #!/bin/bash can only work where there is bash
echo "My name is $1" 
echo "I am $2 years old"
echo "I live in $3" 
...........
bash hello.sh Fawaz 15 Dubai #this code outputs: My name is Fawaz - I am 15 years old - I live in Dubai
-------
.......
#!/bin/bash
cp $1 $2
......
bash copy.sh oldfile.txt newfile.txt # It copies oldfile.txt (which is $1) to newfile.txt (which is $2)

$@   # all arguments as separate words
--------
#!/bin/bash 
echo "Hello to: $@" #it prints individual args in their own sentence, it outputs n number of sentence
echo "Hello to: $*" #groups all args together, it outputs 1 sentence
echo " there are # names" #counts the number args, it outputs the count
-----
bash count.sh apple banana cake juice #output =>You gave me 4 things
............
$?   # exit code of last command (0 = success)
$$   # PID of the current script Process ID
$!   # PID of last background process

Variable       Meaning
$@        All arguments separately    
$*       All arguments as one string 
$#       How many arguments            
$?       Success/fail of last command  # Did it work? (0 = yes)
$$       Current script's Process     
$!       Last background process ID


Declaring & using variables
NAME="Alice"             # no spaces around =
AGE=30
GREETING="Hello, $NAME"  # double quotes expand variables
LITERAL='Cost: $5'     # echo $'literal' will print literal, but $literal  
echo "$NAME is $AGE"    # Alice is 30
echo "${NAME}s party"  # curly braces isolate the var name

ARRAY
FRUITS=("apple" "banana" "cherry")
echo "${FRUITS[0]}"     # apple
echo "${FRUITS[@]}"     # all elements
echo "${#FRUITS[@]}"    # length: 3

Default values & substitution
NAME="${Fawaz:-default}"    # use $1 if set, else "default."
NAME="${Victor:=Fawaz}"  # send fawaz if victor not available
echo "${FAWWAZ:?'Fawwaz is missing '}"  #prints fawwaz is missing if there is no FAWWAZ

Syntax	Meaning
${VAR:-value}	Use value if VAR is empty/unset
${VAR:=value}	Use value and assign it to VAR if empty/unset
${VAR:?message}	Exit with error if VAR is empty/unset
${VAR:+value}	Use value only if VAR is set

A useful way to remember it:

- → substitute
= → substitute and save
? → check and fail if missing
+ → use alternate value when present


